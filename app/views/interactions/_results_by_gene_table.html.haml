.well
  %table#table-by-source.table.table-striped.table-bordered(cellpadding="0" cellspacing="0" border="0")
    %thead
      %tr
        %th Search Term
        %th Gene
        %th Distinct Drug Count (passed filter)
        %th Distinct Drug Count (failed filter)
        %th Druggable Categories List
    %tbody
      =render partial: 'interactions/results_by_gene_table_row', collection: results.interactions_by_gene
