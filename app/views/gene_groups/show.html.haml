#notice= notice
=content_for :title, @title

.container-fluid
  .row-fluid
    .span12
      .hero-unit
        -@gene_groups.each do |gene_group|
          -gene_group.genes.each do |gene|
            %p
              =render :partial => "gene_group_entry", :locals => { :gene => gene }
