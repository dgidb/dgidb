.well
  %table#table-by-source.table.table-striped.table-bordered(cellpadding="0" cellspacing="0" border="0")
    %thead
      %tr
        %th Name
        -gene_group_presenter.source_db_names.each do |name|
          %th=name
    %tbody
      =render partial: 'gene_groups/by_source_table_row', collection: gene_group_presenter.grouped_names
