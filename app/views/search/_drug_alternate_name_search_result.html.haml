.well
  %em Drug Alternate Name
  %p
    %strong=link_to r.alternate_name, drug_path(r.drug.name)
    %p="Drug:  #{r.drug.name} from #{r.drug.citation.source_db_name} version #{r.drug.citation.source_db_version}"
