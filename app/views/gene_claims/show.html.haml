#notice= notice
=content_for :title, "DGIdb - #{@gene_claim.title} Gene Record"
=content_for :header do
  %h2
    %span{id: "gene_name"}
      =@gene_claim.title
    %small 
      ="gene data from #{@gene_claim.source.source_db_name}" 
.container-fluid
  .row-fluid
    .span12
      .well
        %h3 Gene Record
        =render partial: 'gene_claims/gene_claim', locals: {gene_claim: @gene_claim}
