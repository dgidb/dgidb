require 'spec_helper'

describe DataModel::GeneGroupBridge do
  fixtures :all

  #it 'should have a gene' do
  #  gene_name_group_bridge("bridge_c").gene.should be_an_instance_of(DataModel::Gene)
  #end

  #it 'should have a gene_group' do
  #  gene_name_group_bridge("bridge_c").gene_group.should be_an_instance_of(DataModel::GeneGroup)
  #end

end
